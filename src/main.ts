import { bootstrapApplication } from '@angular/platform-browser';
import { InicioComponent } from "./app/componentes/inicio/inicio.component";

bootstrapApplication(InicioComponent).catch(err => console.error(err))
